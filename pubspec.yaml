name: NumberCheckup
description: "A new Flutter project."
publish_to: 'none'
version: 1.0.0+4

environment:
  sdk: ^3.5.2

dependencies:
  flutter:
    sdk: flutter
  http: ^0.13.5
  flutter_rating_bar: ^4.0.0
  intl: ^0.19.0
  url_launcher: ^6.1.7
  cupertino_icons: ^1.0.8
  google_mobile_ads: ^5.1.0
  shared_preferences: ^2.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^4.0.0
  flutter_launcher_icons: ^0.11.0  # Додайте цей пакет для генерації іконок

flutter_icons:
  android: true
  ios: true
  image_path: "assets/icon/app_icon.jpg"  # Вкажіть шлях до вашої іконки

flutter:
  uses-material-design: true
  assets:
    - assets/photo/
